from collections import Counter

class Solution:
    def firstUniqChar(self, s: str) -> int:
        a = len(s)
        count = Counter(s)
        
        for i in range(0, a):
            if count[s[i]] == 1:
                return i 
                
        return -1
