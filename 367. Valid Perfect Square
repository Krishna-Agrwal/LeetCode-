import math
class Solution:
    def isPerfectSquare(self, num: int) -> bool:
        data = math.sqrt(num)
        if data.is_integer():
            return True
        else:
            return False
